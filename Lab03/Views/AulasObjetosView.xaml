<UserControl x:Class="Lab03.Views.AulasObjetosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Catálogo de Aulas" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
            <Border Background="#DCFCE7" CornerRadius="4" HorizontalAlignment="Left" Padding="8,3" Margin="0,5,0,0">
                <TextBlock Text="Modo Conectado (List&lt;Aula&gt; / SqlDataReader)" FontSize="11" FontWeight="SemiBold" Foreground="#166534"/>
            </Border>
        </StackPanel>

        <!-- Barra de Búsqueda -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="12,8" FontSize="13" BorderBrush="#CBD5E1" BorderThickness="1" Background="White" Margin="0,0,10,0">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <Button Grid.Column="1" Content="🔍 Buscar" Command="{Binding BuscarCommand}" 
                    Width="110" Height="38" Foreground="White" FontWeight="Bold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="#4F46E5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Tabla Estilizada -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Aulas}" AutoGenerateColumns="True" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9"
                      Background="Transparent" BorderThickness="0" RowHeight="36">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Height" Value="38"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>